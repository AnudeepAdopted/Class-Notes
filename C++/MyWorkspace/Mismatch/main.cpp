//Here first1 and last1 are the iterators to the first container specifying the range and first2 is the iterator to the 
//second container specifying the position where to start the comparision.
//The elements are by default checked for equality, double equal and the pair of iterator is returned giving the positions
//of elements where mismatch has occured

#include <iostream>
#include <iterator>
#include <algorithm>
#include <vector>

using namespace std;

int main()
{
    
}
